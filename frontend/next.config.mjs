/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'images.lumacdn.com', 'static.vecteezy.com', 'framerusercontent.com']
    }
};

export default nextConfig;
